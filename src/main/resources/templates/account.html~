<!DOCTYPE html>
<html lang="en" xmlns:th="https://www.thymeleaf.org">
<head>
	<meta charset="UTF-8">
	<title>Central Addressing Scheme</title>
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
	      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
<h1 class="text-center">Accounts</h1>

<div class="container col-lg-8 mt-4">
	<a th:href="@{/account/add}" class="btn btn-primary">Add</a>
	<table class="table">
		<thead>
		<tr>
			<th scope="col">#</th>
			<th scope="col">Account Type</th>
			<th scope="col">Currency</th>
			<th scope="col">BIC</th>
			<th scope="col">Opening Date</th>
			<th scope="col">Closing Date</th>
			<th scope="col">Actions</th>
		</tr>
		</thead>
		<tbody>
		<tr th:each="account, iteration : ${accounts}">
			<th scope="row" th:text="${iteration.index + 1}"></th>
			<td th:text="${account.type}"></td>
			<td th:text="${account.currency}"></td>
			<td th:text="${account.servicer.bic}"></td>
			<td th:text="${account.openingDate}"></td>
			<td th:text="${account.closingDate}"></td>
			<td>
				<a th:href="@{/account/edit/{id}(id=${account.id.iban})}" class="btn btn-info">Edit</a>
				<a th:href="@{/account/delete/{id}(id=${account.id.iban})}" class="btn btn-danger">Delete</a>
			</td>
		</tr>
		</tbody>
	</table>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>
</html>