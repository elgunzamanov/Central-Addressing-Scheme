<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Central Addressing Scheme</title>
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
	      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
<h1 class="text-center">Customer Add</h1>

<div class="container col-lg-4 mt-4">
	<form th:action="@{/customer-add}" th:object="${customer}" method="POST">
		<input type="hidden" th:field="*{id}">
		<div class="mb-3">
			<label for="pin" class="form-label">Pin</label>
			<input type="text"  th:field="*{pin}" class="form-control" id="pin">
		</div>
		<div class="mb-3">
			<label for="mobile-number" class="form-label">Mobile number</label>
			<input type="text" th:field="*{contactDetails.mobileNumber}" class="form-control" id="mobile-number">
		</div>
		<div class="form-check">
			<input class="form-check-input" th:field="*{resident}" type="checkbox" value="" id="resident">
			<label class="form-check-label" for="resident">Resident</label>
		</div>
		<button type="submit" class="btn btn-primary">Submit</button>
		<a class="btn btn-secondary" href="/">Back to home</a>
	</form>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>
</html>